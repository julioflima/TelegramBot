{
  "name": "arrowtips",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "clear && nodemon src/server.js",
    "serve": "pm2 kill && pm2 start src/server.js && pm2 monit",
    "production": "pm2 kill && node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "global": "^4.4.0",
    "nodemon": "^2.0.6",
    "telegram-bot-api": "^2.0.1"
  }
}
